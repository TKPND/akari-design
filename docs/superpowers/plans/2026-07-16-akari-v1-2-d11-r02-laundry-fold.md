# Akari v1.2 Daily.3 D11 r02 Implementation Plan

**Goal:** Replace the shared r01 hidden-foot support geometry with a stable
sofa-edge laundry fold that keeps both feet and the full action readable.

## Tasks

1. Add failing r02 request-order, count, contract, and command tests.
2. Register the frozen r02 sofa-edge A/B contract and close r01 as a shared
   scene retry rather than generating C.
3. Generate independent r02 A/B from accepted references only and compare at
   original resolution.
4. Promote the strongest passing candidate byte-for-byte, record all four
   ordered D11 reviews, and verify clean-clone linkage.
5. Pass D11 and full Natural Form gates before and after fast-forwarding main.
