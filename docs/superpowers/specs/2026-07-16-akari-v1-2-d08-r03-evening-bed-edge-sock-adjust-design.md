# Akari v1.2 Daily.2 D08 r03 Screen-Side Retry Design

**Date:** 2026-07-16

**Status:** Approved under the user's autonomous-completion delegation

## 1. Why r02 Closed

D08 r02 removed the sock-height contradiction, but all three independent
candidates placed both hands on image-left, Akari's character-right sock. Their
identity, anatomy, state, support, and rendering passed; the repeated failure is
isolated to ambiguous character-relative left/right wording in the D08 scene.

Core, D07, and the bedroom composition remain frozen. No r01 or r02 candidate
may be used as a generation reference.

## 2. r03 Correction

Keep the r02 action and explicitly bind character sides to the final picture:

- Akari's character-left side is image-right, directly below her visible
  character-left ornament;
- both hands smooth only the image-right character-left sock;
- the untouched image-left character-right sock stays slightly lower;
- both socks keep exactly two complete stripes and continuous knit mouths;
- both feet remain grounded on separate lanes.

The same camera, bed support, outfit, expression, lighting, cuff asymmetry, and
restrained room context remain required.

## 3. Retry Contract

Use revision `r03` with the same D08 descriptor, dependencies, five accepted
references, production gate, and candidate policy. Generate independent A/B
from the screen-coordinate prompt. Optional C remains available only for
eligible candidate-local D08 failures.

Local evidence uses `source/candidates/d08/r03/` and
`comparisons/d08-r03/d08-r03-comparison.webp`. Promotion uses
`accepted/daily/evening/akari-v1.2_d08_evening-bed-edge-sock-adjust_r03.png`.
